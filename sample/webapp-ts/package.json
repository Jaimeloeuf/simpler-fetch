{
  "name": "sample-js-webapp",
  "scripts": {
    "serve": "npx vite --port 8080",
    "preview": "npx vite preview --port 8080",
    "build": "npx vite build"
  },
  "devDependencies": {
    "vite": "^4.4.11"
  },
  "dependencies": {
    "simpler-fetch": "file:../..",
    "zod": "^3.22.4"
  }
}
